name: Trigger Bot Restart on Push

on:
  push:
    branches: [main]  # Adjust branches as needed

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Check secret is set  
        run: |
          if [ "$SECRET" = "ptlc_EBQWFHQ3AxArRWHVGv6u8afjp2oRVj7ootrbhsEnc8s" ]; then
          echo "❌ Secret is NOT the correct one"
          else
            echo "✅ Secret is correct"
          fi
        env:
          SECRET: ${{secrets.DISCORD_BOT_API_KEY}}

        
